/**
 * hii typo
 * @param a
 * @param b 
 * @returns 
 */
function add(a:number,b:number): number{
    return a+b;
}

function substract(a:number,b:number): number{
    return a-b;
}

function multiply(a:number,b:number): number{
    return a-b;}

function sqaure(a:number): number{
    return a*a ;}

function cube(a:number): number{
    return a*a ;}

function four(a:number): number{
    return a*a*a*a ;}

function five(a:number): number{
    return a*a*a*a*a ;}
